DB_HOST=transformhomes-server.mongo.cosmos.azure.com DB_USER=transformhomes-server 
DB_PASS=xcgX44vaSjD0DQluUiklYednVLKx881vvP53A0vnSzrNtBB8BdUfVe3umsz9U7ury6AuVNMiRIumACDbPiM2kQ== DB_NAME=transformhomes-server
# Add .env to .gitignore
echo "tradesman-backend/.env" >> .gitignore
# Stage the changes
git add tradesman-backend/.env .gitignore
# Amend the commit (if it was the most recent commit)
git commit --amend
# Or create a new commit
git commit -m "Remove sensitive data from .env and add to gitignore"
# Try pushing again
git push# Edit the .env file to remove the secret
nano tradesman-backend/.env

# Add .env to .gitignore
echo "tradesman-backend/.env" >> .gitignore

# Stage the changes
git add tradesman-backend/.env .gitignore

# Amend the commit (if it was the most recent commit)
git commit --amend

# Or create a new commit
git commit -m "Remove sensitive data from .env and add to gitignore"

# Try pushing again
git push
